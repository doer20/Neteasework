spring:
  application:
    name: account-service
  cloud:
    config:
      profile: master
      label: default
      discovery:
        enable: true
        serviceId: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/