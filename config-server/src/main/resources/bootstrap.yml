spring:
  application:
    name: config-server
  cloud:
      config:
        server:
          git:
            uri: https://gitee.com/iss2015302580186/NeteaseWork
            search-paths: config-repository
server:
  port: 7000
